/*global define, jQuery, underscore, backbone */
/*jshint esnext: true */
define(['jQuery', 'underscore', 'backbone'], function($, _, Backbone){
  var PictureTileRowView = Backbone.View.extend({
    tagName: 'div',
    className: 'ajmebc-shifting-tile ajmebc-shifting-tile-row',
		initialize: function(){
		},
		render: function(){
		},
  });

  return PictureTileRowView;
});
